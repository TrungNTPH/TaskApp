Task Manager App

Ứng dụng quản lý công việc đa nền tảng (React Native) với các tính năng quản lý task, upload ảnh và CV, thông báo push, và hỗ trợ offline.

🔹 Tính năng

Quản lý Task

Thêm, sửa, xóa task.

Thay đổi trạng thái task (Hoàn thành / Chưa hoàn thành).

Xem chi tiết task, bao gồm avatar và file CV.

Upload & Camera

Chọn ảnh từ thư viện hoặc chụp trực tiếp.

Upload file CV (PDF, Word).

Phân trang & Infinite Scroll

Danh sách task hiển thị theo trang.

Kéo xuống để load thêm trang mới (mỗi trang 50 task).

Offline Mode

Lưu cache task bằng AsyncStorage.

Khi mất mạng vẫn xem được danh sách task.

Thông báo Push

Tích hợp Firebase Cloud Messaging (FCM).

Hiển thị thông báo khi app foreground hoặc background.

Search & Filter

Tìm kiếm theo tên công việc.

Lọc theo giới tính.